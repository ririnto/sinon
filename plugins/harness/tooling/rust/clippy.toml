# Canonical Clippy configuration for the Harness `rust` profile.
# Merge keys into an existing clippy.toml instead of replacing it.
# See docs/tools/rust.md for integration rules.

cognitive-complexity-threshold = 25
too-many-arguments-threshold = 5
type-complexity-threshold = 250
