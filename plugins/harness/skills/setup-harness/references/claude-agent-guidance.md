# Claude Agent Guidance

Open this reference when adapting target repository Claude agent files.

Target agent assets live under `assets/agents/` and are copied as complete runtime agent prompts. Do not add scaffold-only prose to those files. Keep target agent frontmatter to `name`, `description`, and `model` unless a target repository intentionally adds runtime-specific fields after copying.

Claude project agents live under `.claude/agents/`.
