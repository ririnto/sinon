stages:
  - validate

harness:
  stage: validate
  image: debian:stable-slim
  before_script:
    - apt-get update && apt-get install -y python3 shellcheck
  script:
    - >-
      {{validation_command}}
