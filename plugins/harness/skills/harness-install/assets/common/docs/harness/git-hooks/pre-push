#!/usr/bin/env sh
# -*- coding: utf-8 -*-
set -e

printf '%s\n' 'This packaged placeholder is replaced during harness installation.' >&2
printf '%s\n' 'Run install-harness.py so the target hook contains generated pre-commit and pre-push stages.' >&2
exit 1
