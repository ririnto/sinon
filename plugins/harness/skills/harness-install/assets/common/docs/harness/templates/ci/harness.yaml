name: {{tool_name}}

on:
  pull_request:
  push:
    branches:
      - {{default_branch}}

jobs:
  {{tool_name}}:
    runs-on: {{runner}}
    steps:
      - uses: actions/checkout@{{checkout_version}}
      - name: Run {{tool_name}} validation
        run: {{validation_command}}
