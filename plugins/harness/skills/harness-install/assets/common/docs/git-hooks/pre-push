#!/usr/bin/env sh
# -*- coding: utf-8 -*-
set -e

printf '%s\n' 'This packaged placeholder is replaced during installation.' >&2
printf '%s\n' 'Run the installer so the target hook contains active pre-commit and pre-push stages.' >&2
exit 1
