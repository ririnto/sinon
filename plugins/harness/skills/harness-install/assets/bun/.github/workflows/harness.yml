name: harness

on:
  - push
  - pull_request

jobs:
  harness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: >-
          {{validation_command}}
