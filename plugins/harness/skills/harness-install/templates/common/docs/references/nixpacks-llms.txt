Replaceable seed reference for Nixpacks or buildpack deployment context.

Purpose: provide LLM-readable deployment/build context only when the target repository uses Nixpacks or compatible buildpack behavior.
When to update: replace or remove this seed when the deployment stack is known.
Required evidence: cite the deployment config, provider docs snapshot, or build command source used by agents.
Validation link: run the stack-specific harness validation command listed in .claude/harness/README.md after replacing or removing this file.
