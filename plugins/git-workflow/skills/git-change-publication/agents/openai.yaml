interface:
  display_name: "Git Change Publication"
  short_description: "Commit readiness, Conventional Commit drafting, and template-aware PR/MR body drafting"
  default_prompt: "Use $git-change-publication to decide whether changes are ready to commit, draft a Conventional Commit message, prepare hosted review body text, and branch into GitHub or GitLab-specific template handling only when needed."
