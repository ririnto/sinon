interface:
  display_name: "Git Change Publication"
  short_description: "Commit readiness checks, split decisions, Conventional Commit drafting from real repository state, and hosted review body preparation"
  default_prompt: "Use $git-change-publication to decide whether changes are ready to commit, whether they should be split, draft a Conventional Commit message from the real diff, prepare hosted review body text, and branch into GitHub or GitLab-specific template handling only when needed."
