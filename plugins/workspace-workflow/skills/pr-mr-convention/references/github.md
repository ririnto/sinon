# Portable Change Description Reference

Use repository-local instruction files and Git evidence to preserve required title, rationale, changes, validation, review focus, risks, and integration handoff sections.

Do not use hosted-service commands, labels, assignees, reviewer accounts, quick actions, numbered tracker references, or remote metadata mutation.

When a repository supplies a local template, preserve its headings and required order in the Git-contained description.
