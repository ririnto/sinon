interface:
  display_name: "JDK Runtime Diagnostics"
  short_description: "JVM stack, dump, JFR, and runtime triage workflows"
  default_prompt: "Use $jdk-runtime-diagnostics for stack traces, thread dumps, JFR captures, and low-risk JVM triage steps."
