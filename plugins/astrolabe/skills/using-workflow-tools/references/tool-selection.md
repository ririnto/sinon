# Tool Selection

Use CodeGraph for symbol, architecture, caller, impact, and flow questions when a current project index and interface exist.
Use `rg` for exact text, filenames, manifest fields, and prohibited terms.
Use `Read` for known files and for verifying the current text after an indexed exploration.
Use RTK only when its proxy is installed and its reduced output still proves the claim.
