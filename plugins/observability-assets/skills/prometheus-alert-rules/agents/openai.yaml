interface:
  display_name: "Prometheus Alert Rules"
  short_description: "Prometheus alerts, recording-rule-backed alert design, and promtool check rules"
  default_prompt: "Use $prometheus-alert-rules for Prometheus alert design, alert-local query review, recording-rule-backed alert design, and promtool check rules validation."
